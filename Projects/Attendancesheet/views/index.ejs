<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Sheet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
  <div class="container text-center m-3 xs-12 ">
    <% var today=new Date();
     
    %>
    <h2>Attendance Sheet (<%= today.getDate();%>/<%= today.getMonth()+1; %>/<%= today.getFullYear(); %>)</h2>
  </div>
    <table class="table table-hover table-dark xs-12">
        <thead>
          <tr>
            <th scope="col">Roll number</th>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Attendance</th>
          </tr>
        </thead>
        <tbody>
          <% studentlist=data;
              parsedstudentlist=JSON.parse(studentlist);
            for(i=0;i<=parsedstudentlist.length-1;i++){

            %>
          <tr>
            <th scope="row"><%= parsedstudentlist[i].rollnumber%></th>
            <td><%= parsedstudentlist[i].name%></td>
            <td><%= parsedstudentlist[i].email%></td>
            <%if(parsedstudentlist[i].present){%>
            <td>  <input class="form-check-input" type="checkbox" checked disabled></td>
            <%}else{%>
              <td> <input class="form-check-input" type="checkbox"  disabled></td>
              <%}%>
          </tr>
          <% }%>
         
            
        </tbody>
      </table>
</body>
</html>